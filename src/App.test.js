import { render, screen } from "@testing-library/react";
import App from "./App";
describe("App Component", () => {
  it("sample test to make this test suit not fail", () => {
    expect(2 + 2).toBe(4);
  });
});
